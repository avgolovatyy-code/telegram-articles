"""Telegram Bot API integration: rich blocks, product cards, renderer, publisher.

Import submodules directly (``from app.telegram.renderer import RichMessageRenderer``);
see the note in ``app.generation``.
"""
